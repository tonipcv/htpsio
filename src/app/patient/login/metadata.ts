import type { Metadata, Viewport } from 'next'

export const metadata: Metadata = {
  title: 'Login | HTSP',
  description: 'Login to access your HTSP account',
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  themeColor: '#1e40af',
} 